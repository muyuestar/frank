package com.frank.commonslang3;

import org.apache.commons.lang3.ArrayUtils;
import org.apache.commons.lang3.StringUtils;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

/**
 * @author: kst
 * @date: 2024/12/6 14:24
 */
public class StringUtilsTest {

    public static void main(String[] args) {
        // 多元素指定字符拼接
//        String[] arr = new String[]{"apple", "orange", "cherry"};
//        String join = StringUtils.join(arr, "、");
//        System.out.println(join);


        // 两集合元素合并按指定字符输出
//        String[] arr = new String[]{"aaa", "bbb", "ccc"};
//        String[] arr1 = new String[]{"ccc", "ddd", "eee", "fff"};
//        List<String> list = new ArrayList<>();
//        list.addAll(Arrays.asList(arr));
//        Arrays.stream(arr1).filter(ele -> StringUtils.isEmpty(ele)).filter(ele -> !ArrayUtils.contains(arr, ele)).forEach(ele -> list.add(ele));
//        String str = StringUtils.join(list, "、");
//        System.out.println(str);
    }
}
